import 'package:flutter/material.dart';

class TrendNxtScreen extends StatelessWidget {
  const TrendNxtScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('TreaandNxt Screen'),
    );
  }
}
